apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tasks-pv-claim
spec:
  volumeName: tasks-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
